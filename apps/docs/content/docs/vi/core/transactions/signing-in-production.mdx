---
title: Ký trong Môi trường Production
description:
  Nơi lưu trữ khóa ký trong ứng dụng Solana trên môi trường production, và cách
  lựa chọn giữa khóa cục bộ, ví trình duyệt, Cloud KMS/HSM, và backend ký được
  quản lý.
type: conceptual
prerequisites:
  - /docs/core/transactions/transaction-structure
related:
  - /docs/tools/keychain
  - /docs/payments/production-readiness
---

Một giao dịch chỉ được thực thi khi nó mang một
[chữ ký](/docs/core/transactions/transaction-structure#signatures) hợp lệ từ mọi
người ký bắt buộc. Trong quá trình phát triển cục bộ, bạn thường lưu trữ khóa đó
dưới dạng [`Keypair`](/developers/cookbook/wallets/create-keypair) và ký trực
tiếp. Trong môi trường production, câu hỏi quan trọng lại khác: **khóa riêng tư
được lưu ở đâu, và ai được phép ủy quyền chữ ký bằng khóa đó?**

<Callout type="warn">
  Không bao giờ nhúng khóa riêng tư vào mã phía client, đóng gói chúng vào
  frontend, hoặc commit chúng vào source control. Bất kỳ ai có được khóa sẽ có
  toàn quyền kiểm soát tài khoản và số tiền của tài khoản đó.
</Callout>

## Các phương pháp ký

| Phương pháp               | Nơi lưu trữ khóa                                                 | Ai ký                                        | Phù hợp nhất cho                                            |
| ------------------------- | ---------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------- |
| **keypair cục bộ**        | Một file hoặc biến môi trường trên máy                           | Mã của bạn, trực tiếp                        | Phát triển cục bộ, kiểm thử, CI                             |
| **Ví trình duyệt**        | Ví của người dùng cuối (ví dụ: Phantom, Solflare)                | Người dùng cuối, theo từng giao dịch         | Các dApp frontend nơi người dùng tự ký giao dịch của mình   |
| **Cloud KMS / HSM**       | Dịch vụ khóa được hỗ trợ bởi phần cứng (AWS KMS, GCP KMS, Vault) | Backend của bạn yêu cầu chữ ký từ dịch vụ    | Dịch vụ backend, vận hành ngân quỹ, môi trường được quản lý |
| **Ví được quản lý / MPC** | Phân tách hoặc lưu ký thác trên cơ sở hạ tầng của nhà cung cấp   | Nhà cung cấp đồng ký theo chính sách của bạn | Ví nhúng, quy trình phê duyệt, lưu ký thác tổ chức          |

keypair cục bộ là phương pháp duy nhất đưa vật liệu khóa thô vào trong ứng dụng
của bạn. Mọi phương pháp triển khai thực tế đều giữ khóa trong cơ sở hạ tầng
chuyên dụng và yêu cầu cơ sở hạ tầng đó ký thay mặt bạn.

## Ký phía backend

Khi máy chủ cần ký — thanh toán phí, gửi giao dịch chương trình, hoặc vận hành
kho quỹ — hãy sử dụng backend quản lý khóa chuyên dụng thay vì keypair thô.
Solana Foundation duy trì [Keychain](/docs/tools/keychain), một thư viện ký
thống nhất (Rust và TypeScript) cung cấp một giao diện `SolanaSigner` thống nhất
trên mọi backend trong bảng trên: Memory, HashiCorp Vault, AWS KMS, GCP KMS,
Fireblocks, Privy, Turnkey, CDP, Crossmint, Dfns, Para, Openfort, và Utila.

Vì giao diện hoàn toàn giống nhau trên tất cả các backend, bạn có thể phát triển
cục bộ với khóa trong bộ nhớ và chuyển sang backend triển khai thực tế thông qua
cấu hình, mà không cần viết lại mã ứng dụng. Keychain tương thích với
[`@solana/kit`](/docs/clients/official/javascript) và
[Rust SDK](/docs/clients/official/rust).

<Cards>
  <Card title="Keychain" href="/docs/tools/keychain">
    Ký thống nhất trên khóa cục bộ, KMS, HSM và các backend ví được quản lý.
  </Card>
  <Card title="Chọn backend" href="/docs/tools/keychain/choosing-a-backend">
    So sánh các mô hình lưu ký và chọn backend ký phù hợp cho ứng dụng của bạn.
  </Card>
</Cards>

Để xem danh sách kiểm tra triển khai thực tế đầy đủ — quản lý khóa, bảo mật RPC
và giám sát — xem
[Sẵn sàng triển khai thực tế](/docs/payments/production-readiness).
